import { SortObjArrByPipe } from './sort-obj-arr-by.pipe';

describe('SortObjArrByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortObjArrByPipe();
    expect(pipe).toBeTruthy();
  });
});
